![Screenshot of theme](screenshots/preview.png) Add the two `.css` files to
`PROFILE_PATH/chrome/`\
Set `browser.tabs.allow_transparent_browser` and
`toolkit.legacyUserProfileCustomizations.stylesheets` to `true` in
`about:config`
